namespace DnDGame.Domain.Enums;

/// <summary>Rarity tier shown on a Collection entry's card, mirroring the frontend's CollectionItem.rarity union.</summary>
public enum CollectionRarity
{
    Common = 1,
    Uncommon = 2,
    Rare = 3,
    Epic = 4,
    Legendary = 5
}

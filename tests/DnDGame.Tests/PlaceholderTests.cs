using Xunit;

namespace DnDGame.Tests;

// A single trivial test confirming the test project is wired up correctly
// (references resolve, xUnit runs). Replace this with real tests starting
// in Phase 3, once BusinessLayer actually has logic to test — e.g. dice-check
// math, leveling thresholds, and story-branching resolution.
public class PlaceholderTests
{
    [Fact]
    public void TestProject_IsConfiguredCorrectly()
    {
        Assert.True(true);
    }
}
